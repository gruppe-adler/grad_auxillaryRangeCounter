name = "GRAD Pace count beads"
prefix = "grad_paceCountBeads"
author = "Ainer, Salbei"
files = [
    "mod.cpp",
    "Adler.paa",
    "LICENSE",
    "README.md"
]

key_name = "{{prefix}}_{{version}}"
authority = "{{prefix}}_{{version}}-{{git \"id 8\"}}"

releasebuild = [
    "@zip grad_paceCountBeads_{{semver.major}}.{{semver.minor}}.{{semver.patch}}.{{semver.build}}"
]

[header_exts]
version= "{{git \"id 8\"}}"